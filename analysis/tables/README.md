# Analysis Tables

Derived table outputs belong here.

- `run_inventory.csv` — normalized run inventory from the scoreboard + run JSON files
- `summary_table.csv` / `summary_table.md` — grouped summary views used for operator decisions

These files are generated from `tools/` and should not be edited by hand.
