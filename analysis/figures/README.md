# Analysis Figures

Derived figures belong here.

The current tooling writes lightweight Mermaid markdown plots such as `metric_plot.md`.
These are generated from `tools/plot_results.py` and should be treated as derived outputs.
